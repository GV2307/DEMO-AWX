name: Test ARC Runner

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: arc-runner-set

    steps:
      - name: Check runner
        run: |
          echo "Running on ARC!"
          uname -a
          whoami
